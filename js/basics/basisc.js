let userBodyWeight = prompt("Enter your weight (kg)");
let userHeight = prompt("Enter your height (cm)");
let userIndexBodyWeight = Math.round((userBodyWeight / (userHeight*2))*100);

if (userBodyWeight == null){
   console.log("ss")
   
}